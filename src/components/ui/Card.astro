---
const { title, description, tags = [] } = Astro.props;
---
<article class="rounded-3xl border border-slate-800/70 bg-slate-900/80 p-6 shadow-soft">
  <h3 class="text-xl font-semibold text-white">{title}</h3>
  <p class="mt-3 text-slate-300 leading-7">{description}</p>
  <div class="mt-5 flex flex-wrap gap-2 text-sm text-slate-400">
    {tags.map((tag) => (
      <span class="rounded-full border border-slate-700 px-3 py-1">{tag}</span>
    ))}
  </div>
</article>
